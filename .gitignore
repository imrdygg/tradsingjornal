node_modules/
build/
dist/
coverage/

# Playwright e2e artifacts
test-results/
playwright-report/
.DS_Store
*.log
.env*
!.env.example

# This project uses npm, so package-lock.json is the single source of truth.
# Ignore bun's lockfiles so a stray `bun install` can't introduce a divergent one.
bun.lock
bun.lockb
